<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Todo List</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-blue-100 flex justify-center items-center h-screen flex-col">
        <div class="w-1/3 bg-white p-6 shadow-lg rounded-lg text-center">
            <h1 class="bg-green-500 text-white py-3 rounded-t-lg text-lg">Todo List</h1>
            <ul class="mt-4">
                <% todolist.forEach(function(todo, index) { %>
                <li class="bg-white border border-gray-300 my-2 p-3 rounded-lg flex justify-between items-center transition-transform transform opacity-0 translate-x-[-20px] animate-fadeIn"
                    style="animation-delay: <%= index * 0.1 %>s">
                    <span><%- todo %></span>
                    <div>
                        <a href="/todo/<%= index %>" class="text-yellow-500 hover:scale-110 mr-3">✎</a>
                        <a href="/todo/delete/<%= index %>" class="text-red-500 hover:scale-110">✘</a>
                    </div>
                </li>
                <% }); %>
            </ul>
            <form action="/todo/add/" method="post" class="flex flex-col items-center bg-white p-4 rounded-lg mt-4">
                <label for="newtodo" class="text-gray-600 mb-2">What should I do?</label>
                <input type="text" name="newtodo" id="newtodo" autofocus class="w-4/5 p-2 border border-gray-300 rounded-md mb-2 text-gray-700 focus:outline-none focus:ring-2 focus:ring-green-400" />
                <input type="submit" value="Add" class="bg-green-500 text-white px-4 py-2 rounded-md cursor-pointer hover:bg-green-600 transition" />
            </form>
        </div>
        <style>
            @keyframes fadeIn {
                to {
                    opacity: 1;
                    transform: translateX(0);
                }
            }
            .animate-fadeIn {
                animation: fadeIn 0.5s forwards;
            }
        </style>
    </body>
</html>
